import { SearchByIdPipe } from './search-by-id.pipe';

describe('SearchByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
